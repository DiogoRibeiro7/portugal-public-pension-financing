# Raw data

Raw source files belong here and must be immutable once registered.

Recommended structure:

```text
cga/
cge/
legislation/
social_security/
public_employment/
banking_pensions/
european_accounts/
```

Each downloaded file must be registered in `evidence/source_registry.csv` with a SHA-256 hash.
